<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Board">
	<select id="seqNextval" resultType="int"> <!--  select 태그만 resultType이 들어간다 -->
      select board_seq.nextval from dual 
   </select>
	
	<insert id="boardWrite">
		insert into board values (${seq},#{title},#{content},#{writer},sysdate,0)
	</insert>

   <select id="boardList" resultType="kh.spring.dto.BoardDTO">
      select * from (select row_number() over (order by seq desc) as rnum, seq, title, content, writer, write_date, view_count from board) where rnum between ${startNum} and ${endNum} 
   </select>
   
   <select id="getTotalCount" resultType="int">
      select count(*) from board
   </select>





</mapper>